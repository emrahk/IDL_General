#!/usr/bin/ksh

# Get GSFC IDL Astro routines occultation data

machine=`/usr/bin/hostname`
if [ ${machine} != "nickel" ]; then
    echo "This script only to be run on nickel!"
    exit 1
fi
cd /home/wcoburn/bin/mirror
./mirror packages/idlmama  1>/dev/null 2>&1
#
#fin
#

